server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index overlay.html;

  # Permitir embed desde OBS y SSN
  add_header X-Frame-Options "ALLOWALL" always;
  add_header Access-Control-Allow-Origin "*" always;

  location / {
    try_files $uri $uri/ =404;
  }
}
